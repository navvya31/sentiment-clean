name: Hello World Workflow

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    
    steps:        
      - name: Display Hello World
        run: echo "Hello World"
